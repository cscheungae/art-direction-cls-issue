# art-direction-cls-issue

## Run a Lighthouse test against it

## Observation
### When the art direction use a image fallback with `auto` aspect-ratio and `width: 100%; height: auto`. It fails the CLS test in Lighthouse.
